interface BoardDimensions {
  maximumSize: number;
  minimumSize: number;
  initialSize: number;
}

export const boardDimensions:BoardDimensions = {
  maximumSize: 600,
  minimumSize: 30,
  initialSize: 500
};