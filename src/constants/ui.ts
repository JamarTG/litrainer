// responsible for which components are visible in the UI
export const PROMOTION_DIALOG_Z_INDEX = 100;
export const GAMES_DROPDOWN_Z_INDEX = 80;
export const MARKER_Z_INDEX = 80;

// responsible for the size of the marker relative to the board size
export const MARKER_SCALE_FACTOR = 16;
